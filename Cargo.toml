[package]
name = "StringZilla"
version = "0.1.0"

[lib]
path = "rust/lib.rs"
name = "stringzilla"

[build-dependencies]
cc = { version = "1.0.83", features = ["parallel"] }
bindgen = { version = "0.69.1", features = ["experimental"] }

[profile.release]
lto = "thin"
